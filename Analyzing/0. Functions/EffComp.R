EffComp <- function(# Function to compute effect sizes for a \code{statcheck} object
	x,
	### \code{statcheck} object
	){

	if(x$test_statistic=="t"){
		ESComp <- (x$test_statistic^2)/(x$test_statistic^2+)
		} else if(x$test_statistic="F"){

			} else if(x$test_statistic="r"){

				} else if(x$test_statistic="Chi2"){

				}
}