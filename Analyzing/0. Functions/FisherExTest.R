# FisherExTest
# Compute transformed p-values
# Compute Fisher test Statistic
# Compute Fisher test statistic P-value

FisherExTest <- function(x ##<<< Vector of p-values in state space [0;1].
                         id ##<<< Vector giving the unique identifiers per paper){
                         
}